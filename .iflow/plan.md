## 生成 IFLOW.md 文件的计划

基于对项目的全面分析，我将生成一个包含以下内容的 IFLOW.md 文件：

### 主要内容：
1. **项目概述**：花名册管理工具 - Python GUI应用程序（v1.2.1）
2. **技术栈**：Python 3.12, Tkinter, pandas, openpyxl, PyInstaller
3. **项目结构**：完整的文件和目录组织说明
4. **核心功能**：文件夹创建、文件重命名、汇总表生成、清理空文件夹
5. **构建和运行**：
   - 本地开发运行命令
   - 本地构建可执行文件命令
   - GitHub Actions自动化构建流程
6. **开发约定**：代码结构、数据格式、文件命名规则
7. **关键文件说明**：init.py, requirements.txt, init.spec等的作用

### 执行步骤：
1. 使用 `write_file` 工具生成完整的 IFLOW.md 文件
2. 确保 Markdown 格式正确，内容清晰易读
3. 包含所有必要的项目上下文信息

### 升级功能
自适应图片大小，然后在图片的正下方创建一块纯白“水印区域”，把姓名，学号等水印信息放到纯白的“水印区域”，大小要自适应